# ERRATA · 2026-09-18

> **Rev.1.1** — the image-identity pass added rows **33–41** and process failures **6–7**.
> That pass was triggered by a reader-visible contradiction: `README` published an image
> identity of `e541746d26e31a3f` while `start.sh`'s own preflight comment cited
> `4ebef21b…` for the same image. Re-measuring on all four nodes resolved it — see the new
> rows. Nothing in Rev.1.0 was retracted.

This repository was published on 2026-09-17/18 from a working fleet. Several documents
in it were written against **earlier build forms** and were carried into the repo
verbatim. This page is the single index of what was wrong, what it actually is, and
which files were corrected.

**How to read the rest of `docs/`:** documents dated 2026-09-11 / 09-12 / 09-13 are
**historical records** of the Form B configuration (1 M ctx · 5 M KV pool · 12 concurrent ·
fp4 indexer off). Their original values are **preserved on purpose** so the engineering
trail stays auditable; each now carries a banner at the top telling you which values are
superseded and where to read the current ones. Do not "fix" the historical numbers in place.

---

## Summary of corrections

| # | Where | Was | Is | Level |
|---|---|---|---|---|
| 1 | README (both languages) | `--enable-deepseek-v4-fp4-indexer` "tried and reverted" | **enabled in current production** (Form A). Still recorded as reverted for Form B | 🔴 |
| 2 | README (both languages) | `MAX_RUNNING_REQUESTS=12` | **16** | 🔴 |
| 3 | README (both languages) | `NCCL_IB_GID_INDEX=-1` "iron rule" | **`NCCL_IB_GID_INDEX=3`** (`NCCL_IB_GID_INDEX_FORCE=3` in the template) | 🔴 |
| 4 | README (both languages) | Form A and Form B numbers mixed with no era label | split into **§2 Form A** and **§5 Form B**, each tagged | 🔴 |
| 5 | README (both languages) | "the adaptation lives on `4dgx-ring`"; "this fork keeps upstream `main` untouched" | the repo's default branch is **`main`** and the adaptation is **on `main`**; it is not a branch of upstream | 🔴 |
| 6 | README (both languages) | `.env.tp4.ring.example` | file does not exist; the shipped template is **`.env.tp4.example`** | 🔴 |
| 7 | README.zh-CN | "1M ctx / 5M KV 池" as the current form | **600 K / 9.6 M** | 🔴 |
| 8 | BUILD-IDENTITY | image `dsv41-4x-spark:local`, SGLang `e087e662b`, `Recorded 2026-09-13` | **`dsv41-sglang-optimized:v7`**, SGLang **`da64c5cb`** | 🔴 |
| 9 | BUILD-IDENTITY | referenced `README-dsv41.md` | that file does not exist | 🔴 |
| 10 | BUILD-IDENTITY | single md5 set for an image that no longer matches | re-measured against `v7` (`boot.py` md5 changed) | 🔴 |
| 11 | BUILD-IDENTITY | "image identity must match on all 4 nodes" | image IDs **legitimately differ** (head build vs transported form); layer count / image ID are **not** valid acceptance criteria | 🟠 |
| 12 | FINAL-METRICS-600K | "total PR peak **6,319 tps** (PR32768-C16)" | **no source anywhere; withdrawn.** Traceable maxima are prefill 3,436.14 t/s (8192-C8) and common-window total decode 355.40 t/s (32768-C16) — different quantities, not additive | 🔴 |
| 13 | FINAL-METRICS-600K | "prefill peak 3,352 t/s (PR32768-C1)" | **3,436.14 t/s (8192-C8)**; 3,351.81 is just the 32768-C1 cell | 🔴 |
| 14 | FINAL-METRICS-600K | free-form DE baseline 62.3 / 222.6 / 356.1 | **99.40 / 213.52 / 339.02**; the old values appear in **no** raw file | 🔴 |
| 15 | FINAL-METRICS-600K | prose-C16 aggregate 358.6 | **460.04** | 🔴 |
| 16 | FINAL-METRICS-600K | "structured coding C1 **+43.7 %** vs free-form" | **−10.0 %**. The +43.7 % came from an untraceable baseline and is void | 🔴 |
| 17 | FINAL-METRICS-600K | "DE free-form 10 cells" | **15 cells** (3 tasks × 5 concurrencies) | 🟠 |
| 18 | FINAL-METRICS-600K | "GSM8K, 200 questions, greedy" | **temp 0.6, 8-shot** (not greedy); accuracy 0.9600 unchanged | 🟠 |
| 19 | FINAL-METRICS-600K | fp4-indexer GSM8K reported only as "107/108" | raw accuracy is **0.535** (107/200, err=92); both figures must appear together | 🟠 |
| 20 | FINAL-METRICS-600K | gateway "non-streaming +156.4 %, do not use :8001" | after the same-day `relay_plain` streaming fix it is **+4.7 %** | 🔴 |
| 21 | FINAL-METRICS-600K | contained real internal fabric subnets | replaced with `<FABRIC_SUBNET_A>` / `<FABRIC_SUBNET_B>` per the sanitization policy | 🔴 |
| 22 | FINAL-METRICS-600K | "PR/DE complete data in `data/`" | at the time only the structured matrix was there; **PR 30-cell and DE 15-cell archives added** | 🔴 |
| 23 | deployment plan (09-11) | proxy on port **8888** | **8001** (nothing listens on 8888) | 🔴 |
| 24 | deployment plan (09-11) | node IPs `.55/.56/.57/.58` | `.186/.187/.188/.189` | 🔴 |
| 25 | deployment plan (09-11) | `CONTEXT 262144` / `MAX_TOTAL_TOKENS 2M` | **600000 / 9600000** | 🔴 |
| 26 | deployment plan (09-11) | "763 B MoE" | **≈550 B** (computed from the shipped `config.json`) | 🔴 |
| 27 | deployment plan (09-11) | `--served-model-name deepseek-v4-flash-vision-exp` | **`deepseek-v4.1-flash`** | 🟡 |
| 28 | upstream survey (09-13) | fp4 indexer "was the culprit, removed" | enabled in current production | 🔴 |
| 29 | upstream survey (09-13) | `context 1,048,576` / KV 4,999,936 / 12 requests | 600000 / 9,600,000 / 16 | 🔴 |
| 30 | upstream survey (09-13) | "recommend `MAX_RUNNING_REQUESTS` back to 8" | current form runs 16 with a fully pinned pool | 🔴 |
| 31 | upstream survey (09-13) | claimed the site SGLang commit matched `e59e6eb…` | site commit is **`da64c5cb`** | 🟠 |
| 32 | `.env.tp4.example` | `CONTEXT_LENGTH=262144` + a comment asserting 600 K × 16-way is unreachable | production runs `CONTEXT_LENGTH=600000` with `MAX_TOTAL_TOKENS=9600000`; stale comment replaced | 🔴 |
| 33 | README (both languages) | image identity `e541746d26e31a3f` "(must match on all 4 nodes)" | **`4ebef21b6aedbd70`** — the value all four nodes actually report *and* the value `start.sh`'s own `image_preflight()` asserts. `e541746d…` reproduces on no node and is referenced by no production script | 🔴 |
| 34 | BUILD-IDENTITY | `c8751accc458138c` published as a reproducible digest | **ghost value.** It appeared next to a formula that actually yields `0050285e87c6f408`; the repo contradicted itself against `start.sh:818`, whose comment already cites `4ebef21b…` | 🔴 |
| 35 | BUILD-IDENTITY | the identity formula was not pinned | three equally plausible one-liners give **three different values** for the same image. The authoritative formula is now quoted verbatim from `start.sh` (`IMGID_TPL`, line 777) with each variant and its value listed side by side | 🔴 |
| 36 | BUILD-IDENTITY | "the image ID legitimately differs across nodes" with no stated mechanism | mechanism pinned by measurement **and** by the launcher's own comment (`start.sh:772-776`): the head runs the containerd `overlayfs` snapshotter, the workers run classic `overlay2`, and the two report different `.Id` for identical content. `.Config` `77799e6ce3de5824`, `.RootFS` `cdd980943dfe9e6b`, `.Created` `0203e92cacf2a896` and every `org.dsv41.*` label are **byte-hash-identical on all four nodes** | 🟠 |
| 37 | README (both languages) | "workers use a squashed form" | workers receive the **same 123-layer content**; only the *reported* image ID differs. No squash is involved | 🟠 |
| 38 | BUILD-IDENTITY | base image listed as if present fleet-wide | `lmsysorg/sglang:dev-dsv41` is present on **01 and 02 only** (02 reports a different ID over the same 70 layers) and is **absent on 03/04** | 🟠 |
| 39 | BUILD-IDENTITY | launch line missing two flags | `--random-seed 0` and `--api-key` were omitted; the line is now transcribed from the live `/proc/1/cmdline` in captured order | 🟡 |
| 40 | `start.sh` (pre-existing) | the comment above `IMGID_TPL` cited layer-list hash `38bbe8265458…` **while the code three lines below asserts `4ebef21b6aedbd70`** | `38bbe8265458f328` is real but belongs to a *different* pipeline (full-JSON `docker inspect` → line-by-line print → `sha256sum`). The comment now names the authoritative value and warns that a hash is meaningless without its byte-exact pipeline | 🔴 |
| 41 | `start.sh`, `BUILD-IDENTITY` | real node hostnames in a public repo, contradicting §8's own claim that hostnames are placeholdered | replaced with generic node labels (`node01…node04`, "head (rank 0)"). **Injected by this very cleanup** in BUILD-IDENTITY's verification recipe, and pre-existing in `start.sh:773` | 🔴 |

---

## What changed in the repository

| file | action |
|---|---|
| `README.md`, `README.zh-CN.md` | rewritten: Form A / Form B split, error fixes, complete PR + DE matrices inline; **§7 image-identity corrected** (`4ebef21b6aedbd70` + the authoritative command) |
| `BUILD-IDENTITY.md` | rewritten against the live `v7` deployment; **then revised again** with the four-node content-identity table, the pinned formula, the formula-sensitivity and empty-input warnings, the build labels, and the corrected base-image / launch-line facts |
| `docs/03-final-metrics/FINAL-METRICS-600K-2026-09-18.md` | Rev.1.0 → **Rev.2.0**: complete 30-cell PR + 15-cell DE tables, recomputed comparison, §10 errata table |
| `data/pr-matrix-20260917/` | **new** — 30-cell PR archive incl. common-window tables |
| `data/de-freeform-20260917/` | **new** — 15-cell free-form archive + per-cell raw records |
| `docs/4DGX-DSV41-Flash-部署方案-20260911.md` | historical doc, values preserved; banner added |
| `docs/4DGX-dsv41-上游调研-20260913.md` | historical doc, values preserved; banner added |
| `docs/4DGX-dsv41-基准测试-横向对比-20260912.md` | historical doc, values preserved; banner added |
| `.env.tp4.example` | context/pool corrected, stale warning comment replaced |
| `start.sh` | **preflight comment corrected** — it cited a layer-list hash (`38bbe8265458…`) that its own adjacent code contradicts, and carried a real node hostname; both fixed, no code path changed |

---

## Failures of process worth recording

1. **A summary table was written before its raw data existed.** The free-form DE baseline
   values (62.3 / 222.6 / 356.1 / 358.6) are present in the narrative but in no archive.
   Whatever produced them is not in the repo or on the fleet. The lesson is generic:
   *a number without a file behind it is not a measurement.*
2. **A "peak" was picked from one cell instead of searched across the matrix.** The PR
   prefill peak was quoted from 32768-C1 without comparing the other 29 cells.
3. **A composite metric was invented by adding two incommensurable quantities.** Prefill
   throughput and common-window decode throughput describe different intervals of the
   same run; summing them is meaningless.
4. **A conclusion outlived its measurement.** The gateway verdict was published from the
   pre-fix numbers and not re-derived after the fix landed the same day.
5. **Numbers were cited with a currency the data did not have.** "greedy" for a
   temperature-0.6 run; "107/108" for a run whose raw accuracy is 0.535.
6. **A hash was published whose formula was missing — and then a *second*, incompatible
   hash was published next to a formula that produced a third value.** This is worse than
   a wrong number, because a hash *looks* like proof. Two rules fall out of it:
   * a hash is only meaningful together with the **byte-exact pipeline** that produced it
     (the same layer list hashes to `4ebef21b6aedbd70`, `bb7c2d4b38af0514` and
     `0050285e87c6f408` under three "obviously equivalent" one-liners), and
   * an identity check that reads **absent input** must fail closed. `sha256(b"")` =
     `e3b0c44298fc1c14` and `sha256(b"\n")` = `01ba4719c80b6fe9` are both well-formed
     strings that mean *"nothing was there"* — they are the same family of defect as the
     "false ABSENT / false empty" class this fleet keeps hitting.
7. **Two files in the same repository disagreed about the same identity, and neither was
   treated as authoritative.** `README` said `e541746d…` while `start.sh`'s own preflight
   comment said `4ebef21b…`. A published identifier that no working code path agrees with
   should have been a release blocker, not a documentation nit.

**Rule adopted from here on:** every number in a published table must be re-derivable
from a file in `data/` by an independent reader, and every table must carry the build
form it was measured on. **Corollary for identifiers:** every hash must ship with its
exact command, and the value used by production code is the only one that may be called
*the* identity.

---

*Compiled 2026-09-18 by the engineering-assurance team. All "Is" values were re-measured
on the live fleet; all corrected aggregates were recomputed from the raw archives with
Python, not by hand. Rev.1.1's identity values were measured on all four nodes with the
exact formula quoted, and the release artifact's md5 was recomputed from the local
14,463,467,578-byte copy — both hash **and** byte count match the published table.*
